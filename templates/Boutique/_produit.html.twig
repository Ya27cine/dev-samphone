
<div class="card mb-4">
        <div class="card-body">
                <h5 class="card-title">
                        <a href="{{  path('boutique.show', {slug: produit.slug, id: produit.id}) }}">
                                {{ produit.titre }}
                        </a>
                </h5>
                <P class="card-text">{{ produit.descri }}</P>
                <div class="text-primary" style="font-size: 2rem; font-weight: bold">
                        {{ produit.formattedPrix }}€
                </div>
        </div>
</div>